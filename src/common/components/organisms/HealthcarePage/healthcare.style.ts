'use client'

import styled from 'styled-components';
import { health_props } from '.';

export const HealthCareWrapper = styled.div<health_props>`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-size: 20px;
`