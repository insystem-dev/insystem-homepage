'use client'

import styled from 'styled-components';
import { si_props } from '.';

export const siWrapper = styled.div<si_props>`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-size: 20px;
`