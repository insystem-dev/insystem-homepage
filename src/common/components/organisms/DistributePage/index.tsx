"use client";

import React from "react";
import * as S from "./si.style";

export type DistributeProps = {};

const DistributePage = ({}: DistributeProps) => {
  return <S.DistributePage>페이지 준비중입니다.</S.DistributePage>;
};

export default DistributePage;
